## [26. Walk (AtCoder Educational DP Contest R)](https://atcoder.jp/contests/dp/tasks/dp_r)

Тема: матричне піднесення в степінь, лінійний сталий перехід, бінарне піднесення

[Код](https://atcoder.jp/contests/dp/submissions/78249755)

Ключові ідеї:

- `A[i][j] = 1`, якщо є ребро `i -> j`. Тоді `A^k[i][j]` = кількість шляхів рівно з `k` ребер з `i` в `j`.
- Відповідь = сума всіх `n*n` елементів `A^K`, бо старт і фініш довільні.
- Доведення індукцією: розкладаємо шляхи по **передостанній вершині** `m` (вона в кожного шляху одна). Купки не перетинаються і покривають усе, тому сумуємо по `m`. Купка для `m` порожня, якщо нема ребра `m -> j`; інакше її розмір = кількість шляхів `i -> m` довжини `k-1`. Одним виразом: `f(k,i,j) = sum_m f(k-1,i,m) * A[m][j]` — це буквально означення множення матриць.
- Множник `A[m][j]` тут працює як вимикач: нема ребра — доданок зникає.
- Бінарне піднесення: `K = 13 = 1101 = 8+4+1`, тому `A^13 = A^8 * A^4 * A^1`. Заготовки будуємо квадруванням — множення не додає 1 до показника, а **подвоює** його, тому ~60 множень дотягують до `2^60 ≈ 1e18`.
- Схема з числами переноситься на матриці дослівно, бо потрібні лише три факти, і всі три виконуються: множення асоціативне, `x^a * x^b = x^(a+b)`, є нейтральний елемент.

Помилки:

- Плутав одиничну матрицю з матрицею з самих одиниць Одинична — одиниці **тільки на діагоналі**; 

Складність: `O(N^3 log K)` час, `O(N^2)` пам'ять.

## [27. Tower (AtCoder Educational DP Contest X)](https://atcoder.jp/contests/dp/tasks/dp_x)

Тема: exchange argument, порядок сортування з невідомим фоном, 0/1 knapsack, розкриття min у нерівностях

[Код](https://atcoder.jp/contests/dp/submissions/78318247)

Ключові ідеї:

- Задача розпадається на дві незалежні: (1) в якому порядку ставити вибрані блоки, (2) який набір блоків вибрати. Другу без першої розв'язати не можна, бо DP потребує фіксованого порядку обходу.
- Сортувати окремо по `w` або окремо по `s` — обидва неправильно. Жоден з них не враховує сумарний ефект: скільки блок витримує ВІДНОСНО того, скільки він сам додає ваги нижнім.
- Правильний ключ сортування — `w + s`. Більший `w + s` лежить нижче. Сортую за зростанням `w + s`, і тоді прохід по масиву зліва направо = рух по башті зверху вниз.
- Після сортування вага вже взятих блоків — це рівно те, що тисне на наступний блок згори. Тому вона й стає параметром стану.

Виведення порядку (exchange argument):

- Беру два сусідні блоки `A` і `B` і невідомий фон `X` — сумарну вагу всього, що лежить над цією парою. `X` я не контролюю, він залежить від решти набору.
- Купа `XBA` (`A` найнижче): на `B` тисне тільки `X`, на `A` тисне `X` плюс сам `B`. Умови: `s_A >= w_B + X` і `s_B >= X`. Разом: `min(s_B, s_A - w_B) >= X`.
- Купа `XAB` (`B` найнижче), симетрично: `s_B >= w_A + X` і `s_A >= X`. Разом: `min(s_A, s_B - w_A) >= X`.
- Припускаю, що `A` краще ставити нижче. Це означає, що його запас витримує все, що витримує другий порядок:

```
min(s_B, s_A - w_B) >= min(s_A, s_B - w_A)
```

- Розкриваю правий мінімум. Мінімум СПРАВА → достатньо ОДНІЄЇ з гілок (АБО):

```
1)  min(s_B, s_A - w_B) >= s_A
2)  min(s_B, s_A - w_B) >= s_B - w_A
```

- Тепер у кожній гілці мінімум опинився ЗЛІВА → потрібні ОБИДВІ кон'юнкти (І).
- Гілка 1: `s_B >= s_A` І `s_A - w_B >= s_A`. Друга кон'юнкта рівносильна `w_B <= 0`, що суперечить умові `w_B >= 1`. Перша при цьому цілком може бути істинною, але кон'юнкції це не рятує. Гілка відпадає.
- Гілка 2: `s_B >= s_B - w_A` І `s_A - w_B >= s_B - w_A`. Перша істинна завжди, бо `w_A >= 1`. Друга рівносильна `s_A + w_A >= s_B + w_B` — це і є єдина змістовна умова.
- Отже вся нерівність рівносильна `s_A + w_A >= s_B + w_B`. Більший `w + s` — нижче, тому в сортуванні за зростанням `w + s` він стоїть пізніше.
- Це доводить лише порядок СУСІДІВ. Перехід до всієї башти — стандартний обмінний аргумент: будь-яку допустиму башту серією обмінів сусідів приводжу до відсортованого порядку, не ламаючи допустимість і не змінюючи суму цінностей. Отже серед оптимальних розв'язків завжди є відсортований.

Правила розкриття min (те, чого мені бракувало):

- `min(p, q) >= C` рівносильно `p >= C` І `q >= C`. Мінімум малий, щоб він перевищив `C`, обидва аргументи мусять перевищити.
- `p >= min(q, r)` рівносильно `p >= q` АБО `p >= r`. Мінімум — це одне з двох чисел, достатньо перевершити менше.
- Практичний рецепт: у кожній гілці шукаю, чи є варіант, що виконується задарма (з невід'ємності ваг). Якщо є — гілка безкоштовна. Якщо нема — саме вона й дає змістовну умову.
- Аналогічно з max, тільки напрямки дзеркальні.

Інваріант:

- Після обробки перших `i` блоків (у відсортованому порядку) `dp[j]` = максимальна цінність допустимої башти, складеної тільки з цих `i` блоків, сумарна вага якої `j`. Допустимої означає: кожен блок цієї башти витримує все, що над ним.
- `dp[j] = -1` означає, що така вага недосяжна допустимою баштою. `dp[0] = 0` — порожня башта.

Патерн для майбутнього:

- Якщо в задачі треба вибрати порядок і жоден очевидний ключ (`w`, `s`, `v`) не працює — беру рівно два сусідні елементи, вводжу невідомий фон `X` (все, що зовні пари), виписую умову допустимості кожного з двох порядків як обмеження на `X`, і порівнюю ці обмеження. Ключ сортування випадає сам.
- Фон `X` вводити обов'язково. Без нього пара розглядається ізольовано, і критерій виходить неправильний.
- Умова допустимості з кількох обмежень майже завжди згортається в `X <= min(...)`. Тому вміння розкривати min — не технічна дрібниця, а робочий інструмент для цього класу задач.
- Ніколи не віднімати нерівності одна від одної. Додавати односпрямовані можна, віднімати — ні.

Складність:

- `O(n log n + n * maxS)` час, `O(maxS)` пам'ять. При `n = 1000` і `maxS = 1e4` це ~1e7.

## [28. Grid 2 (AtCoder Educational DP Contest Y)](https://atcoder.jp/contests/dp/tasks/dp_y)

Тема: комбінаторика на сітці, біноміальні коефіцієнти за модулем, DP по перешкодах, розбиття за першим "поганим" об'єктом

[Код](https://atcoder.jp/contests/dp/submissions/78465577)

Ключові ідеї:

- `H, W <= 1e5` дає до `1e10` клітинок, тому DP по сітці неможливий. `N <= 3000` дає `N^2 = 9e6`. Отже стан DP — **стінка, а не клітинка**.
- Кількість шляхів `(r1,c1) -> (r2,c2)` без перешкод = `C((r2-r1) + (c2-c1), r2-r1)`, і `0`, якщо `r2 < r1` або `c2 < c1`.
- Виведення цієї формули: будь-який шлях робить рівно `d = r2-r1` кроків вниз і `q = c2-c1` вправо — це не залежить від конкретного шляху, бо рядок мусить вирости рівно на `d`, стовпець на `q`, і жоден крок їх не зменшує. Далі бієкція «шлях ↔ слово з букв D і R»: зі шляху слово виписується однозначно, зі слова шлях відновлюється однозначно. Слово довжини `d+q` повністю визначається позиціями букв D, звідси `C(d+q, d)`.
- Головна ідея: **не** включення-виключення по підмножинах стінок (`2^N`), а класифікація кожного **поганого** шляху за **першою** стінкою, яку він відвідує. Перша стінка в шляху рівно одна, тому класи не перетинаються і покривають усе 
- `f[i]` = кількість шляхів `(1,1) -> (r_i, c_i)`, які до `i` не заходять у жодну іншу стінку (тобто `i` — перша стінка на шляху).
- Рекурентність: беру всі шляхи в `i` і віднімаю ті, що вже зачепили стінку раніше, розклавши їх по першій стінці `j`:

```
f[i] = paths(1, 1, r_i, c_i) - sum_j f[j] * paths(r_j, c_j, r_i, c_i)
```

- Чому добуток: шлях ріжеться в точці `j` на дві незалежні частини — префікс `(1,1) -> j` без стінок раніше (це рівно `f[j]`) і суфікс `j -> i`, **вільний**, бо умова «перша стінка = `j`» стосується тільки того, що було ДО `j`. Префікс і суфікс склеюються однозначно.
- Напрямок: **pull** — `f[i]` збирає значення з уже готових `f[j]`.
- Порядок обчислення: сортую стінки лексикографічно за `(r, c)`. Якщо `j` може стояти перед `i` на монотонному шляху, то `r_j <= r_i` і `c_j <= c_i` при різних клітинках, тобто або `r_j < r_i`, або `r_j = r_i` і `c_j < c_i` — у будь-якому разі `j` строго менша лексикографічно і стоїть у масиві раніше. Значить на момент обчислення `f[i]` усі потрібні `f[j]` готові.
- Явну перевірку «чи `j` лівіше-вище за `i`» писати не треба: при `r_j > r_i` або `c_j > c_i` сама `paths(...)` повертає `0`.
- Трюк, що прибирає окрему фінальну формулу: додаю `(H, W)` як фіктивну стінку номер `N+1`. Після сортування вона остання (координати максимальні), і `f[N+1]` за означенням = кількість шляхів у фініш, які не зачепили жодної справжньої стінки. Це і є відповідь, віднімати нічого не треба.

Патерн для майбутнього:

- Коли треба відняти «погані» об'єкти і включення-виключення вибухає — шукати параметр, який у кожного поганого об'єкта **єдиний**. «Перша стінка на шляху» — саме такий: він розбиває погані шляхи на неперетинні класи, і замість `2^N` виходить `N^2`. Ознака, що параметр правильний: він визначений однозначно і кожен поганий об'єкт потрапляє рівно в один клас.
- Це той самий прийом, що в [задачі 26 (Walk)](https://atcoder.jp/contests/dp/tasks/dp_r) — там розклад шляхів по **передостанній вершині**, вона теж у кожного шляху одна. Варто тримати цей прийом як окремий інструмент: «розклади по єдиному характеристичному елементу».

Складність: `O(N^2)`

## [29. Permutation (AtCoder Educational DP Contest, T)](https://atcoder.jp/contests/dp/tasks/dp_t)

Тема: DP, підрахунок перестановок, DP по рангах, префіксні суми

[Код](https://atcoder.jp/contests/dp/submissions/78677555)

Ключові ідеї:
- Усі обмеження задачі — це порівняння сусідів. Тому конкретні значення чисел не несуть жодної інформації: якщо взяти будь-які `i` різних чисел і розставити їх у тому самому відносному порядку, усі знаки виконуються так само. Отже стан треба будувати на порядку, а не на значеннях.
- Щоб перевірити наступний знак, з усього порядку префікса мені потрібен лише останній елемент — точніше, наскільки він великий серед елементів префікса, тобто його ранг.
- Стан: `dp[i][j]` = скільки є відносних порядків елементів на позиціях `1..i` таких, що перші `i-1` знаків рядка `s` виконуються, а елемент на позиції `i` має ранг `j` серед цих `i` елементів. Ранг 1 = найменший.
- База `dp[1][1] = 1`.
- Коли `i = N`, пул — це вже всі `N` елементів, а ранг серед усіх `N` і є саме значення. Тому відповідь = сума `dp[N][j]` по `j = 1..N`.

Правильна модель:
- Напрямок pull: беру валідний порядок довжини `i` і дивлюсь на його префікс довжини `i-1`, який лежить у `dp[i-1][k]`.
- `j` міряється серед `i` елементів, `k` — серед `i-1` елементів. Це різні пули, і саме в цьому вся суть переходу.
- Елемент на позиції `i-1` при цьому не змінюється — змінюється лише пул, у якому я міряю його ранг. Один доданий елемент може посунути ранг максимум на одну сходинку і посуває рівно тоді, коли новий елемент менший. Тобто ранг старого елемента у великому пулі дорівнює `k`, якщо новий більший, і `k+1`, якщо новий менший.
- Знак сам каже, який із двох випадків. `<` означає, що новий елемент більший → ранг старого у великому пулі = `k`, умова `k < j`, тобто `k` пробігає `1..j-1`.
- `>` означає, що новий елемент менший → ранг старого у великому пулі = `k+1`, умова `k+1 > j`, тобто `k` пробігає `j..i-1`.
- Переходи: для `'<'` `dp[i][j]` = сума `dp[i-1][k]` по `k = 1..j-1`; для `'>'` `dp[i][j]` = сума `dp[i-1][k]` по `k = j..i-1`.
- Дублів немає: з кожного порядку довжини `i` виходить рівно один префікс довжини `i-1` і рівно одне `j`, і назад з пари (префікс, `j`) відновлюється рівно один порядок довжини `i`.

Інваріант (реалізація):
- Тримаю не сам шар, а його префіксні суми: `ps[t]` = сума `dp[i-1][k]` по `k = 1..t`, і обов'язково `ps[0] = 0`.
- Шар `i-1` має рівно `i-1` рангів, тому повна сума шару лежить у `ps[i-1]`. Для `t` більших за довжину шару `ps[t]` просто повторює повну суму — хвіст константний, і це ок, поки я звертаюсь за правильним індексом.
- Переходи в такому вигляді: `'<'` → `nPs[j] = ps[j-1]`; `'>'` → `nPs[j] = ps[i-1] - ps[j-1]`.

Патерн для майбутнього:
- Тригер "порахувати перестановки, де заданий патерн зростань/спадань": усі обмеження — порівняння, отже конкретні значення зайві, стан будувати на ранзі останнього елемента.
- Прийом переходу: додаю новий елемент і питаю, як зсунеться ранг попереднього. Один доданий елемент зсуває ранг рівно на 0 або 1, і умова задачі сама каже, на скільки саме.

## [30. Frog 3 (AtCoder Educational DP Contest, Z)](https://atcoder.jp/contests/dp/tasks/dp_z)

Тема: DP, оптимізація DP, Convex Hull Trick, нижня обгортка прямих, монотонний дек, амортизований аналіз

[Код](https://atcoder.jp/contests/dp/submissions/78679834)

Ключові ідеї:
- Зовнішній цикл іде по `i` — на кожному кроці рахую `dp[i]`, вартість дійти до каменя `i`. Всередині перебір по всіх попередніх каменях `j < i`, з яких можна стрибнути: `dp[i] = min по j<i ( dp[j] + (h[j]-h[i])^2 + C )`. Це O(N^2), при `N` до `2*10^5` не проходить, і саме цей внутрішній перебір я і хочу прибрати.
- Розкриваю квадрат: `(h[j]-h[i])^2 = h[j]^2 - 2*h[j]*h[i] + h[i]^2`. Тоді під мінімумом стоїть `dp[j] + h[j]^2 - 2*h[j]*h[i] + h[i]^2 + C`.
- Ключовий наголос: доданки `h[i]^2` і `C` не залежать від `j` взагалі. Для внутрішнього перебору вони просто константа — однакова для всіх кандидатів. Константу можна винести за `min`: `dp[i] = h[i]^2 + C + min по j<i ( dp[j] + h[j]^2 - 2*h[j]*h[i] )`.
- Тепер мінімум шукаю тільки по решті, тобто по `dp[j] + h[j]^2 - 2*h[j]*h[i]`. І ця решта має вигляд `k*x + b`, якщо покласти `k[j] = -2*h[j]`, `b[j] = dp[j] + h[j]^2`, `x = h[i]`.
- Тобто кожен вже оброблений камінь `j` — це не число, а ПРЯМА `y = k[j]*x + b[j]`. А крок DP — це запит «мінімум серед усіх накопичених прямих у точці `x`».
- Чому точкою запиту є h[i]: бо x — це не якась вигадана величина, це просто висота з вхідних даних. Уся задача крутиться навколо масиву h, вартість стрибка визначається виключно висотами двох каменів і більше нічим. Тому коли я рахую перехід у камінь i, підставляю h[i] — головний вхідний параметр, від якого залежить відповідь.
А те, що кандидат j при цьому перетворюється в пряму, — вже вторинне, особливість саме цієї задачі. Так склалося, що квадрат різниці розкладається зручно: частина від j збирається в k і b, а h[i] лишається простим множником. Прямі — це форма запису, у яку лягла вартість, а не суть задачі. Суть — висоти.
Звідси і монотонність: x пробігає ті самі числа h, що й нахили, лише зі зсувом на крок. Тому зростання запитів — не окрема умова, а те саме зростання h, з якого береться спадання нахилів. Одна властивість входу дає обидві монотонності.
- Впорядкованість, на якій все тримається: `h` строго зростає, тому `k[j] = -2*h[j]` строго спадає. Значить прямі, додані пізніше, стрімкіші — крутіше йдуть вниз. Стрімкіші виграють при великих `x`, тобто справа. Виходить, порядок прямих у деку зліва направо збігається з порядком їхнього панування зліва направо. Плюс `x = h[i]` теж зростає від кроку до кроку. Дві монотонності одразу — тому вистачає деку і O(N).
- Причина, чому взагалі можна щось викидати: дві прямі перетинаються рівно один раз. Якщо одна обійшла іншу, вона обійшла назавжди, повернень у лідери не буває.

Крок алгоритму, по порядку:
- Спершу препроцешу голову деку, і аж потім беру мінімум. Поки в деку хоча б два елементи і друга пряма в точці `x` дає не більше за першу — роблю `pop_front`. Викидаю фізично, а не просто пропускаю, бо `x` надалі тільки зростає: якщо голова вже програла сусідці тут, то правіше вона програватиме й поготів
- Після цієї чистки мінімум — це рівно голова деку. Нічого перебирати не треба: `mn = k * x + b` для `dq[0]`.
- На базі мінімуму рахую `dp`: `dp[i] = mn + h[i]^2 + C`. Це та сама константа, яку виносив за `min`, тепер повертаю її назад.
- На базі `dp` будую нову лінію: `k = -2*h[i]`, `b = dp[i] + h[i]^2`. Камінь `i` тепер сам стає кандидатом для наступних.
- Перед вставкою чищу хвіст. Викидаю останню лінію з деку, поки її точка перетину з передостанньою лежить правіше, ніж точка перетину нової лінії з передостанньою. 
- Порівнюю перетини навхрест, без ділення, через `__int128` — саме це й робить функція `bad`.
- Аж тоді `push_back` нової лінії.
- Амортизація: попри два вкладені `while`, кожна пряма додається рівно один раз і видаляється щонайбільше один раз. Сумарно O(N), а не квадрат.

Правильна модель:
- У деку лежать ПРЯМІ, тобто пари `(k, b)`. Точок перетину в деку немає взагалі, ніде.
- Злам — це просто число, x-координата.

Модифікації цієї задачі:
- Лінії впорядковані, а `x` у довільному порядку. Обгортку будую точно так само (чистка хвоста нікуди не дівається, вона спирається лише на монотонність нахилів). А от викидати з голови ЗАБОРОНЕНО: наступний `x` може виявитись меншим і знову захотіти найпершу пряму. Тому тримаю обгортку у векторі й на кожен запит шукаю потрібну пряму заново — бінарним пошуком. Разом O(N log N).
- Бінарний пошук тут можливий тому, що зламú відсортовані: лідери змінюються строго по порядку, кожна пряма панує на одному суцільному відрізку, отже місця передачі влади йдуть зліва направо. Два способи, і змішувати їх не можна. Або тримаю окремий масив зламів і роблю `upper_bound` по числах — кількість зламів, менших за `x`, і є індексом потрібної прямої. Або не тримаю нічого і бінарюсь по індексу з предикатом «чи наступна пряма в точці `x` вже краща за поточну» — тоді злам щоразу відтворюється цим порівнянням, і дробів взагалі немає.
- І лінії, і `x` у довільному порядку. Тоді обгортку не побудуєш одним проходом — нова пряма може лягти в середину, а не в хвіст. Варіанти: Li Chao tree (сегментне дерево по координаті `x`, у кожному вузлі одна пряма-кандидат, вставка й запит за O(log N)) або динамічна обгортка на `set` зі вставкою в середину і чисткою сусідів в обидва боки.

Патерн для майбутнього:
- Якщо в переході DP є добуток «щось від `i`» на «щось від `j`» — це сигнал CHT. Пробую записати перехід у вигляді `dp[j] + a[j]*b[i]` і подивитись, чи виносяться решта доданків за `min`.
- Перевіряю дві монотонності ОКРЕМО: нахили і точки запиту. Від того, які з них є, залежить структура — дек, вектор з бінпошуком, або Li Chao.
- Дек не впаде і не кине асерт, якщо монотонність порушена — він просто мовчки дасть неправильну відповідь. Тому сортованість перевіряю по умові, а не сподіваюсь помітити на тестах.

Складність: O(N) часу, O(N) пам'яті.

## [31. Intervals (AtCoder Educational DP Contest W)](https://atcoder.jp/contests/dp/tasks/dp_w)

Тема: DP по префіксу, стан = позиція найправішої одиниці, дерево відрізків з лінивим проштовхуванням, додавання на відрізку + максимум на префіксі, обробка відрізків за правим кінцем

[Код](https://atcoder.jp/contests/dp/submissions/78717525)

Ключові ідеї:

- Відрізок `(l, r, a)` — нелокальний доданок, розтягнутий у часі: в момент `l` про нього ще нічого не відомо. Прив'язую його до одного моменту — до `r`. Саме в `r` питання «чи є одиниця в `[l, r]`» стає остаточним: раніше одиниця ще може з'явитись, пізніше вже нічого не зміниться.
- Стан: `p` = позиція найправішої одиниці в префіксі `1..i`, `p = 0`, якщо одиниць ще нема. Весь рядок `1..i` можна забути й пам'ятати одне це число.
- Чому саме найправіша: відповідь на «чи є одиниця в `[l, i]`» монотонна по `l`. Коли `l` зменшується, вікно тільки росте вліво, одиниця звідти не зникає — тому «ні, ні, ні, так, так» і ніколи не в шахматку. Одне перемикання → весь стовпчик відповідей для всіх `l` стискається в одне число-поріг, і цей поріг = найправіша одиниця. Перевірка: `10001` і `00001` дають однакові відповіді для будь-якого відрізка з `r = 5` — зайва одиниця на позиції 1 не додає нічого, бо все, що лівіше найправішої, вже всередині будь-якого вікна, яке дотягнулось так далеко вліво.
- Умова спрацювання відрізка `(l, i, a)` через стан: `p >= l`. Значення `p` розпадаються на два суцільні шматки: `p` в `[l, i]` — плюс `a`, `p` в `[0, l-1]` — нічого. Тому один вхідний відрізок = одне діапазонне додавання по `dp[l..i]`. Звідси сегментка, і вона тут не оптимізує формулу переходу, а обслуговує масові оновлення.
- Перехід: `dp[i] = max(dp[0..i-1])`. Ліві одиниці могли зібрати лише відрізки з `r < i` — вони вже остаточно пораховані. Постановка одиниці в `i` жодного з них не втрачає і жодного нового не додає, тому лівий і правий шматки незалежні: беру найкращий лівий.
- Порядок усередині кроку `i`: спочатку максимум префікса, потім додавання відрізків з `r = i`. Навпаки не можна — в максимум потрапили б комірки `dp[l..i-1]`, які вже отримали `+a`, а для них цей відрізок ще не визначений, і `a` зарахувалось би вдруге.
- Відповідь: `max(dp[0..N])`. `dp[0] = 0` — це рядок з самих нулів, тому випадок «краще нічого не ставити» виходить сам.

Інваріант:

- Після кроку `i` комірка `dp[p]` = найкращий бал серед усіх рядків довжини `i`, у яких найправіша одиниця стоїть на позиції `p`, з урахуванням рівно всіх відрізків з `r <= i`.

Де затупив:

- Шукав стан у звичному вигляді «як далеко я дійшов» — індекс, сума, лічильник. Тут такий стан некоректний у принципі: `dp[i]` на префіксі не визначене, бо внесок майбутнього залежить від того, що конкретно стоїть позаду.
- Не додумав, що стисле минуле — це позиція найправішої одиниці. Спостереження «одиниці можуть бути на різних позиціях» у мене було, але я зупинився і не перевірив головного: чи ця різниця взагалі помітна ззовні.
- Формулював поріг як «найлівіша позиція після `l`, де є одиниця» — тобто прив'язано до `l`, своя відповідь для кожного `l`. А треба було одне число, незалежне від `l`.
- Вирішив, що переходу взагалі не потрібно: мовляв, відрізки з `r < i` на крок `i` не впливають, а майбутні прийдуть масовими апдейтами. Через це моя модель описувала тільки рядки рівно з ОДНІЄЮ одиницею — комірку `dp[p]` чіпали лише відрізки з `l <= p <= r`, тобто ті, що спрацьовують від однієї одиниці на `p`. Контрприклад, який це вбиває за 20 секунд: `N = 4`, `(1,2,10)`, `(3,4,10)` — моя модель дає 10, правильна відповідь 20 (рядок `1010`).
- Наслідок тієї ж помилки: думав «лівіша одиниця може дати більше за правішу, тому просто беру максимум». Правильний висновок з цього спостереження інший — навіщо обирати, якщо можна поставити обидві.
  
Патерн для майбутнього:

- Якщо доданок у сумі — нелокальний об'єкт (відрізок, пара, шлях), питаю: у який момент він стає остаточно визначеним? Прив'язую його до цього моменту і лише до нього. Це та сама ідея, що в Grid 2 (розбиття поганих шляхів за першою стінкою) — другий раз поспіль.
- Коли «індекс + лічильник» не працює, питаю не «який індекс взяти за стан», а «який мінімальний конспект минулого достатній, щоб порахувати все майбутнє». Перевірка конкретна: беру два різні минулі й дивлюсь, чи розрізняє їх хоч один майбутній запит. Не розрізняє — це один стан.
- Якщо відповідь на запит монотонна по параметру, весь набір відповідей стискається в одне число-поріг. Шукати цей поріг.
- «Переходу не треба» — червоний прапорець. Перевіряю модель на мінімальному прикладі з двома незалежними блоками (щось ліворуч + щось праворуч, що не перетинаються). Саме такий приклад ловить модель, яка вміє описувати один об'єкт, а не послідовність.
- Якщо в структурі даних параметр `int`, а я передаю туди накопичену відповідь — перевіряти тип одразу.

Складність: `O((N + M) log N)` часу, `O(N)` пам'яті.

## [32. Subtree (AtCoder Educational DP Contest, V)](https://atcoder.jp/contests/dp/tasks/dp_v)
Тема: дерева, rerooting, DP на дереві, префіксні та суфіксні добутки, модульна арифметика, складений модуль
[Код](https://atcoder.jp/contests/dp/submissions/78749530)

Ключові ідеї:
- Якщо `v` чорна, а сусід `u` білий, то весь бік `u` мусить бути білим (інакше чорна вершина звідти сполучалася б з `v` шляхом через білу `u`). Тобто "цей бік білий" — це рівно 1 варіант, а не якийсь підрахунок.
- Підвішую за `1` і тримаю дві величини:
  - `down[v]` — розфарбування піддерева `v` за умови, що `v` чорна.
  - `up[v]` — розфарбування всього, що поза піддеревом `v` (множина `V \ subtree(v)`), за умови, що `v` чорна.
- `down[v] = добуток по дітях c від (down[c] + 1)`. Для кожної дитини: біла — 1 варіант, чорна — `down[c]` варіантів.
- `up[v] = 1 + up[p] * (добуток (down[c]+1) по всіх дітях p, крім v)`, де `p` — батько `v`. `+1` — це випадок "`p` біла", тоді весь зовнішній бік білий. Другий доданок — випадок "`p` чорна": незалежно комбінуються `up[p]` і всі інші гілки `p`.
- База `up[1] = 1`.
- Відповідь `ans[v] = down[v] * up[v]` — обидві частини незалежні, бо єдиний зв'язок між ними йде через саму `v`, а вона зафіксована чорною.
- Обидві формули однакові за структурою: `+1` в них означає одне й те саме — "цей бік білий".
- Порядок обчислення: `dfs1` post-order (pull, знизу вгору) рахує `down`; `dfs2` pre-order (push, зверху вниз) рахує `up` — на вході в `p` вже відомі `up[p]` і всі `down[c]`.

Помилки:
- Хотів дістати "добуток по всіх дітях `p`, крім `v`" через ділення: `down[p] / (down[v]+1)`. Застряг на питанні "як ділити по модулю, якщо `M` не просте".
- Це було невдале питання. Проблема не в тому, що `M` складене. Навіть при простому `M` множник `down[v]+1` може бути `≡ 0 (mod M)` — оберненого не існує взагалі, а добуток решти вже не відновити, бо весь добуток обнулився. Ділення тут тупик у принципі.
- Не зорієнтувався, що "добуток усіх, крім одного" — це стандартна конструкція з префіксних і суфіксних добутків, і ділення в ній не виникає взагалі.
- Як воно робиться: для дітей `c[0..k-1]` кладу `a[i] = down[c[i]] + 1`, рахую `pref[i] = a[0]*...*a[i-1]` (`pref[0]=1`) і `suf[i] = a[i]*...*a[k-1]` (`suf[k]=1`). Тоді добуток усіх, крім `a[i]`, це `pref[i] * suf[i+1]`. Кожен з цих двох — добуток реально перемножених елементів, нічого не "вилучається" постфактум.

Патерн для майбутнього:
- Побачив "добуток (або сума) всіх, крім одного" по модулю — одразу префікс/суфікс, а не ділення. Ділення по модулю — крайній засіб, і легально лише коли `M` просте І множник гарантовано не `≡ 0 (mod M)`.
- Rerooting-шаблон, який тепер знаю в обличчя: `down` знизу вгору за один dfs, `up` зверху вниз за другий; у другому dfs для кожної вершини будую `pref`/`suf` по списку дітей і роздаю `up` дітям.
- У задачах на "чорні вершини зв'язні" відразу шукати спостереження "білий сусід ⇒ весь його бік білий" — воно перетворює цілу гілку на множник `+1`.

Складність: O(n) за часом і пам'яттю 

## [33. Banknote (AtCoder NDPC, D)](https://atcoder.jp/contests/ndpc/tasks/ndpc2026_d)

Тема: цифрове ДП, віднімання в стовпчик, заміна змінних

[Код](https://atcoder.jp/contests/ndpc/submissions/78890047)

Ключові ідеї:
- Заміна змінних: `A = M`, `B = M - N`. Обмеження `M >= N` перетворюється на `B >= 0` і зникає з поля зору. Лишається одне рівняння: `A - B = N`, `A, B >= 0`, мінімізувати `ds(A) + ds(B)`.
- Після заміни це буквально віднімання в стовпчик: `A` — купюри, які я даю, `B` — купюри решти, `N` — те, що вийшло в стовпчику.
- У стовпчику розряди зв'язані рівно однією річчю — чи позичив цей розряд одиницю зі старшого. Скільки цифр я витратив у молодших розрядах — на старші не впливає ніяк. Один біт зв'язку → ДП по цифрах.

Правильна модель:
- Розряд — позиція цифри, рахую справа наліво з нуля. Іду від молодшого (одиниці) до старшого.
- `dp[i][c]` = мінімальна сума цифр `A` і `B` у розрядах `0 .. i-1`, за умови що `c = 1`, якщо розряд `i` винен одиницю (з нього позичили), інакше `c = 0`.
- Старт: `dp[0][0] = 0`, `dp[0][1] = INF`.
- Перехід у розряді `i`: `d` — цифра `N`, `a` і `b` — цифри `A` і `B`, які я обираю, `c'` — чи позичаю зі старшого. Рівняння стовпчика: `a - b - c + 10*c' = d`, тобто `a - b = v - 10*c'`, де `v = d + c`.
- Щоб отримати задану різницю двох цифр найдешевше — одну з них роблю нулем:
  - `c' = 0`: `a = v`, `b = 0`, вартість `v`
  - `c' = 1`: `a = 0`, `b = 10 - v`, вартість `10 - v`
- Кінець: цифри `N` скінчилися. Якщо `c = 1` — треба поставити `1` у старший розряд `A`, це `+1`. Відповідь `min(dp[len][0], dp[len][1] + 1)`.

Патерн для майбутнього:
- Якщо в умові мінімум/максимум по вільному параметру — перше, що роблю, це заміна змінних, щоб параметр зник і лишилося рівняння-обмеження. Тут `M` → `A, B` з `A - B = N`. Поки параметр у формулюванні, я його вгадую замість того, щоб рахувати.
- Якщо задача про цифри і сусідні розряди зв'язані лише переносом або позикою — це ДП по розрядах зі станом в один біт, а не конструкція. Ознака: «зон» може бути скільки завгодно, але вони не взаємодіють між собою.

Складність: `O(18 * 2)` на тест, `O(T * 18)` разом.

